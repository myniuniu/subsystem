# 组织培训分类计数显示进行中数量功能

## 功能描述
根据【进行中项目默认显示规范】，修改了组织培训分类右边的数字显示逻辑，现在显示的是正在进行中的培训数量，而不是总的培训数量。

## 修改内容

### 1. 计数逻辑更新
**修改前**: 显示所有组织培训的总数量
```javascript
const orgCount = notes.filter(note => 
  note.courseType === 'organizational_training' || 
  note.tags?.includes('组织培训') ||
  note.category === 'organizational_training' ||
  note.source === '组织培训'
).length;
```

**修改后**: 显示进行中状态的培训数量
```javascript
// 组织培训分类显示进行中的数量
const orgTrainingNotes = notes.filter(note => 
  note.courseType === 'organizational_training' || 
  note.tags?.includes('组织培训') ||
  note.category === 'organizational_training' ||
  note.source === '组织培训'
);

// 筛选出进行中的培训
const inProgressCount = orgTrainingNotes.filter(note => {
  const statusInfo = getTrainingStatusInfo(note);
  return statusInfo && statusInfo.status === TRAINING_STATUS.IN_PROGRESS;
}).length;
```

### 2. 智能状态识别
- 使用 `getTrainingStatusInfo()` 工具函数
- 基于当前时间与培训开始/结束时间的对比
- 准确识别 `TRAINING_STATUS.IN_PROGRESS` 状态

## 功能特性

### 1. 精准计数
- **智能识别**: 自动识别培训的实际状态
- **实时更新**: 随着时间变化，计数会自动更新
- **准确反映**: 显示真正需要关注的进行中培训数量

### 2. 用户体验提升
- **快速了解**: 用户一眼就能看到有多少个培训正在进行
- **重点突出**: 强调当前需要关注的培训项目
- **减少认知负荷**: 不被已结束或未开始的培训干扰

### 3. 与默认筛选逻辑一致
- 侧边栏显示进行中数量
- 主区域默认显示进行中的培训
- 保持界面信息的一致性

## 测试验证步骤

### 1. 基础计数测试
1. 进入小黑屋页面
2. 查看左侧"🏢 组织培训"分类
3. 确认右边数字显示的是进行中培训的数量
4. 与主区域显示的培训卡片数量进行对比验证

### 2. 状态变化测试
1. 生成模拟数据，确保包含不同状态的培训
2. 检查侧边栏计数是否只统计进行中的培训
3. 如果有培训时间过期，确认计数会相应减少

### 3. 数据一致性测试
1. 打开筛选开关，查看全部状态的培训
2. 手动计算进行中培训的数量
3. 与侧边栏显示的数字进行对比

### 4. 实时更新测试
1. 修改系统时间（如果可能）
2. 观察计数是否随状态变化而更新
3. 验证状态机的准确性

## 预期效果

### ✅ 信息精准度提升
- 侧边栏数字准确反映进行中培训数量
- 用户可以快速了解当前工作负荷
- 避免被无关的培训信息干扰

### ✅ 界面一致性
- 侧边栏计数与主区域默认显示保持一致
- 都聚焦于进行中的培训项目
- 提供统一的用户体验

### ✅ 决策支持
- 帮助用户快速评估学习任务
- 便于制定学习计划和时间安排
- 提高学习效率和时间管理

## 示例场景

### 场景1：多个培训状态混合
假设有以下组织培训：
- 【组织培训】新教师教学方法培训 - 🔄 进行中
- 【组织培训】班级管理实务 - 🔄 进行中  
- 【组织培训】教育技术应用实践 - ✅ 已结束
- 【组织培训】课程设计与开发 - 📅 未开始
- 【组织培训】心理健康指导 - 🔄 进行中
- 【组织培训】信息技术与教学创新 - 📅 未开始

**显示结果**: 侧边栏显示 "🏢 组织培训 (3)"，表示有3个进行中的培训

### 场景2：时间变化影响
当某个进行中的培训到达结束时间后：
- 状态自动变更为"已结束"
- 侧边栏计数自动减1
- 主区域默认不再显示该培训

## 技术实现亮点

### 1. 智能状态计算
```javascript
const statusInfo = getTrainingStatusInfo(note);
return statusInfo && statusInfo.status === TRAINING_STATUS.IN_PROGRESS;
```

### 2. 实时响应性
- 每次数据更新都会重新计算
- 无需手动刷新页面
- 状态变化立即反映在界面上

### 3. 性能优化
- 计算逻辑简洁高效
- 避免重复的状态判断
- 利用已有的工具函数

## 相关文件
- `/src/components/SmartNotes.jsx` - 修改了组织培训分类的计数逻辑
- `/src/utils/trainingStatusUtils.js` - 提供状态判断功能

## 后续优化建议
1. 可以考虑在hover时显示详细的状态统计（如：进行中3个，已结束1个，未开始2个）
2. 可以添加不同状态的颜色标识
3. 可以考虑添加状态变化的动画效果

根据【进行中项目默认显示规范】，此修改让用户能够更精准地了解当前需要关注的培训数量，提升了信息的准确性和实用性。